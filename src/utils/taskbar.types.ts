import { EdgeActionType, TerminalActionType } from '@store/index'

export type IconTypes = 'windows' | 'edge' | 'chrome' | 'terminal'
export type IconActionTypes = TerminalActionType | EdgeActionType
